# fpc-build

Canonical location for shared umbrella toolchain assets.

Native product builds should resolve their default FPC config from `libs/SharedPasCore/fpc-build`. The old `pas/fpc-build` location is legacy cleanup only and should not be used as the default path.
